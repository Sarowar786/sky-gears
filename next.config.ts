import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
};
module.exports = {
  images: {
    domains: ['example.com'], // Add domains for external images here
  },
};

export default nextConfig;
